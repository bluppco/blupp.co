---
const {

    title,
    description

} = Astro.props
---
<div class="w-full bg-zinc-100 rounded-2xl flex flex-col gap-4 p-10 border border-black">
    <p class="text-xl font-medium">{ title }</p>
    <p>{ description }</p>
</div>
