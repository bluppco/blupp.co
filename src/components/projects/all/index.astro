---
// IMPORTS ASTRO LAYOUTS
import Container from "@/layouts/container/index.astro"

// IMPORTS ASTRO COMPONENTS
import ProjectCard from "../card/index.astro"

// IMPORTS ASTRO ATOMS
import HeadingOne from "@/atoms/headings/one/index.astro"

const { data } = Astro.props
---
<Container>
    <section class="py-4 md:py-10 space-y-4 md:space-y-10 pt-28 md:pt-10">
        <HeadingOne>Projects</HeadingOne>
        <section class="grid md:grid-cols-2 gap-4 md:gap-10">
            {

                data.map( ( value ) => {

                    return(
                        <ProjectCard
                            name={ value.name }
                            slug={ value.slug }
                            src={ value.cover }
                            alt={ value.name }
                            github_link={ value.github_link }
                            project_link={ value.project_link }
                            is_client={ value.is_client }
                            notion_setup={ value.notion_setup }
                            is_private={ value.is_private }
                            in_progress={ value.in_progress }
                        />
                    )

                })

            }
        </section>
    </section>
</Container>
