---
// IMPORTS CONSTANTS
import { IMAGE_QUALITY, IMAGE_WIDTH_600 } from "@/constants/images"

const runtime = Astro.locals.runtime
const {

	CDN,
	PROJECT

} = runtime.env

const { src } = Astro.props
---
<picture class="bg-zinc-200">
    <source media="(max-width: 375px)" srcset={ CDN +  src + "?height=300&quality=" + IMAGE_QUALITY } />
    <source media="(max-width: 640px)" srcset={ CDN +  src + "?height=400&quality=" + IMAGE_QUALITY } />
    <img
        src={ CDN + PROJECT + "/" + src + "?height=650&quality=" + IMAGE_QUALITY + "&width=" + IMAGE_WIDTH_600 }
        class="w-full h-full object-cover scale-100 group-hover:scale-110 duration-700 overflow-hidden"
    />
</picture>
